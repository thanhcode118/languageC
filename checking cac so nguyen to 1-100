#include <stdio.h>
#include <math.h>
//so nguyen to la so lon hon 2 va co uoc la 1 va chinh no
int prime(int n){
   if (n<2) return 0;
   for (int i=2;i<=sqrt(n);i++){
   if(n%i==0) return 0;    
   }
    return 1;
}
int main(){
  int n=100;
  // scanf("%d",&n);
for (int i=0;i<n;i++){
    if(prime(i)) printf("%d ",i);
}
}
